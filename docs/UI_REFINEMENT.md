# Console 02 界面重做与验收

本轮重做导航与页面结构，完整替换上一版 `src/ui-refinement.css`，未增加第三层样式文件。只调整展示，不修改升级协议、校验、授权门禁或设备端源码。

## 结构变化

| 维度 | Olive UI 01 | Console 02 |
|---|---|---|
| 桌面导航 | **左侧竖栏** | **顶部横向七分区** |
| 顶栏 | **浅色品牌栏** | **深绿顶栏与02标记** |
| 首页主视觉 | **小图标与短标题** | **双行大标题与圆环图形** |
| 检测区域 | **浅色卡片** | **深绿独立操作区** |
| 升级确认 | **固件卡片内** | **独立第三步操作条** |
| 手机检测 | **按钮在状态卡片后** | **风险提示后直接检测** |
| 预览 | 手机折叠 | 同上 |
| 升级门禁 | 保持不变 | 同上 |

共性：不连接或刷写真机。
关键差异：导航和确认区已实际重排。

## 本地检查

| 维度 | 页面检查 | 协议回归 |
|---|---|---|
| 结果 | **899/899通过** | **53/53通过** |
| 环境 | **Edge同源iframe** | **Node模拟设备** |
| 覆盖 | **14种CSS视口宽度** | **原有传输安全边界** |
| 真机访问 | 未执行 | 同上 |

共性：自动测试不代表真机验收。
关键差异：布局通过不等于审美验收。

视口宽度：320、390、520、521、640、768、900、901、1024、1050、1051、1180、1280、1440 CSS像素。iframe高度为800像素。

新增检查包含：Console 02资源版本、顶栏实际颜色、品牌与连接按钮不重叠、横向导航、独立确认区、风险说明与检测按钮顺序、窄屏检测按钮首屏可见、滚动后确认按钮不被导航遮挡。保留七页导航、横向溢出、折叠交互、下载SHA-256核验后仍受门禁、运行与资源错误等原有检查。

所有浏览器测试均在应用脚本执行前拦截蓝牙设备请求，并取消原有检测确认框。确认按钮遮挡测试只测量布局，不执行刷写。

## 截图评审与修正

- 旧版截图评审指出竖向导航占空间、卡片内文字密集。
- 新版桌面首页截图评审确认横向导航清晰，未发现文字重叠。
- 手机截图暴露介绍区过长、检测按钮位于首屏下方，已缩短介绍并前移检测操作。
- 一次升级截图评审未返回文字，该次不作为通过证据。
- 视觉模型意见是辅助参考；最终观感仍以用户在实际设备上的体验为准。

## 复现

通过项目HTTP(S)地址打开 `tests/browser-smoke.html`；`?width=390` 可单独检查手机宽度。展开“查看逐项检查结果”获取详细断言。

```powershell
node --check src/app.js
node --test --test-reporter=spec tests/ota.test.mjs tests/ota-boundaries.test.mjs
```

## 发布与边界

- 页面样式与入口脚本使用 `?v=console-02`；顶部显示 `02` 标记。
- 本文记录本地验收；线上状态须以实际GitHub Pages资源与测试结果为准。
- 未验证真实手机触摸、锁屏、后台和蓝牙传输。
- 未修改固件、unified-tool、弹道核心或OTA传输实现。
